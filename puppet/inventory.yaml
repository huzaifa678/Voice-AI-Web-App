groups:
  - name: gpu
    targets:
      - uri: REPLACE_WITH_EC2_PUBLIC_DNS_OR_IP
        name: voice-ai-gpu
    config:
      transport: ssh
      ssh:
        user: ubuntu
        private-key: ~/.ssh/voice-ai.pem
        host-key-check: false
        run-as: root
